export function saveWorkflow(workflow: Record<string, unknown>) {
  // TODO: Persist user-approved workflow configuration.
  return { saved: false, workflow, reason: "Workflow persistence is not implemented yet." };
}
