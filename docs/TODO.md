# TODO

## Next 10 Priorities
1. Connect dashboard visuals to `recommendedVisuals`
2. Build chart engine renderer
3. Build confidence score UI
4. Add package-aware deliverables preview
5. Add report generator
6. Improve missing-value coding controls
7. Add branding profiles to export outputs
8. Build AI Analyst panel from analytics plan context
9. Add report library
10. Add unit tests once a TypeScript test runner is selected

## Analytics Engine Follow-Ups
- Connect dashboard KPI cards to `recommendedKpis`.
- Replace local chart heuristics with `visualRecommendationEngine` output.
- Add package-aware deliverables preview from `recommendedDeliverables`.
- Build chart engine renderer.
- Build confidence score UI and detail panels.
- Build report generator for HTML, PDF, DOCX, PPTX, PNG, JSON, and ZIP.
- Add browser-safe engine bundle or build step if Studio should import TypeScript modules directly.

## Documentation Follow-Ups
- Keep pricing documentation aligned with the Studio pricing page.
- Keep analytics engine documentation aligned with source code.
- Add implementation status after each major Studio release.
- Add screenshots or diagrams once the UI stabilizes.
